import React from 'react';
import ReactDOM from 'react-dom/client';

import X_AUTH_APP from './app';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <X_AUTH_APP></X_AUTH_APP>
);

