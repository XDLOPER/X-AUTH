import React from 'react';
import ReactDOM from 'react-dom/client';
import X_AUTH_SETUP from './app';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <X_AUTH_SETUP></X_AUTH_SETUP>
);

